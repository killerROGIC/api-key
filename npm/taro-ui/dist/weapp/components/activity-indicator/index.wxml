<view class="{{rootClass}}" wx:if="{{$taroCompReady}}">
    <view class="at-activity-indicator__body">
        <at-loading compid="{{$compid__250}}"></at-loading>
    </view>
    <text class="at-activity-indicator__content" wx:if="{{content}}">{{content}}</text>
</view>
